"use client";

/**
 * Framer Motion via LazyMotion — import daqui (não de "framer-motion" direto)
 * quando o componente estiver dentro de <MotionProvider />.
 */
export { m, AnimatePresence, LayoutGroup } from "framer-motion";
export type { MotionProps } from "framer-motion";
