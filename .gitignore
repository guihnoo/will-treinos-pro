# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local env files
.env*.local
.env
.env.development.local
.env.test.local
.env.production.local

# vercel
.vercel
.cursor/mcp.json

# typescript
*.tsbuildinfo
next-env.d.ts

# java (present in folder)
/oracleJdk-26

# Supabase CLI local cache
supabase/.temp/

# next-pwa (gerados em `next build` — a Vercel recria no deploy)
public/sw.js
public/sw.js.map

# Arquivos temporários de secrets (NUNCA commitar)
.env.ci-secrets.tmp
.env.vercel.pull
.env.vercel.production
*.secret
*-secret-temp.*
*-secret.*
.cron-secret-temp.txt
*.cron-secret*
public/workbox-*.js
public/workbox-*.js.map
public/fallback-*.js
public/fallback-*.js.map
public/worker-*.js
public/worker-*.js.map

# Design reference (material local de referência — não vai para o GitHub)
/DESIGN_REFERENCE

# Claude Code — permissões/caminhos locais da máquina
.claude/settings.local.json

# Prints temporários para debug com IA (mantém só o README no Git)
.agent-arsenal/debug-screenshots/*
!.agent-arsenal/debug-screenshots/README.md

.vercel
.env*.local
